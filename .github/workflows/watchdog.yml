name: Very Good Films watchdog
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: production
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Check health and confirmed media IDs
        id: health
        run: npm run health
      - name: Open or update incident issue
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue list --state open --label automation --search "publisher watchdog" --json number --jq '.[0].number' | {
            read number || true
            if [ -n "$number" ]; then gh issue comment "$number" --body "Watchdog still failing at $GITHUB_RUN_ID"; else gh issue create --title "publisher watchdog: no confirmed post" --label automation --body "The five-minute watchdog detected a stalled or unhealthy publisher. Run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"; fi
          }
