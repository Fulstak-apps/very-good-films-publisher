{"feeds":[],"allowed_media_hosts":[],"note":"Add an HTTPS JSON scene feed and its explicitly trusted media hosts. Feed schema: docs/SCENE_FEED.md. Local catalog entries are also ingested. Discovery suggestions never imply clip availability."}
